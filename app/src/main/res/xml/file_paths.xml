<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- App-specific external download directory: Context.getExternalFilesDir(Environment.DIRECTORY_DOWNLOADS) -->
    <external-files-path
        name="external_download_files"
        path="Download" />

    <!-- App-specific all external files -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- Public external Download folder -->
    <external-path
        name="external_public_download"
        path="Download" />

    <!-- Public external storage -->
    <external-path
        name="external_storage"
        path="." />

    <!-- External cache directory: Context.externalCacheDir -->
    <external-cache-path
        name="external_cache_files"
        path="." />

    <!-- Internal cache directory: Context.cacheDir -->
    <cache-path
        name="internal_cache_files"
        path="." />

    <!-- Internal app files directory: Context.filesDir -->
    <files-path
        name="internal_files"
        path="." />
</paths>
